/// User-facing app name. Native display names (Windows exe/window, Android
/// label, iOS/macOS bundle names) are set separately in each platform folder.
const appName = 'T.N.D.R.';
const appTagline = 'The Naggy Wife Reminder';
