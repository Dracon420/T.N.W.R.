// Template. In the Alexa developer console, create lambda/config.js from this
// and fill it in there. Never commit the real config.js: the repo is public.
module.exports = {
  // Supabase -> Project Settings -> API -> Project URL
  SUPABASE_URL: 'https://YOUR-PROJECT.supabase.co',
  // Any long random string; must equal the ALEXA_PAIR_SECRET Supabase secret.
  PAIR_SECRET: 'CHANGE-ME',
};
